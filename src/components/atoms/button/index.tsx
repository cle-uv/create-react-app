import React, { FC } from "react";

import styles from "./style.module.scss"; // Import css modules stylesheet as styles

const Button: FC = () => <button className={styles.button}>Button</button>;

export default Button;
